<div class="container py-4">
  <div class="row">
    <div class="col-8 mx-auto">
      <div class="card">
        <div class="card-body">
          <h4 class="title fw-bold">Random Password Generator <i class="fa-solid fa-key"></i></h4>
          <hr>
  
          <form method="POST">
            <!-- Password Length -->
            <div class="form-group row">
              <label for="passwordLength" class="col-md-4 col-form-label">Password Length</label>
              <div class="col-md-8">
                {{!-- 如果input type是'text'或'number', 可以用<input>原有的value屬性, 將使用者填寫的內容代會表單欄位 --}}
                <input type="number" min="4" max="16" class="form-control" id="passwordLength" name="passwordLength" value="{{options.passwordLength}}">
                <p class="hint" style="color:#888888; font-size:small">Length is available between 4 ~ 16</p>
              </div>
            </div>
  
            <!-- Checkbox -->
            <div class="form-group row mb-2 align-items-center">
              <label for="lowercase" class="col-md-4 col-form-label">Include Lowercase Characters</label>
              <div class="col-md-8">
                {{!-- 如果使用者勾選checkbox, 搭配handlebars提供的if語法, html會新增'checked'屬性, 伺服器會收到 { input name的內容: 'on' } --}}
                <input type="checkbox" class="custom-control-input" id="lowercase" name="lowercase" {{#if options.lowercase}}checked{{/if}}>
                <label class="custom-control-label" for="lowercase">(e.g. abcdefg)</label>
              </div>
            </div>

            <div class="form-group row mb-2 align-items-center">
              <label for="uppercase" class="col-md-4 col-form-label">Include Uppercase Characters</label>
              <div class="col-md-8">
                <input type="checkbox" class="custom-control-input" id="uppercase" name="uppercase" {{#if options.uppercase}}checked{{/if}}>
                <label class="custom-control-label" for="uppercase">(e.g. ABCDEFG)</label>
              </div>
            </div>

            <div class="form-group row mb-2 align-items-center">
              <label for="numbers" class="col-md-4 col-form-label">Include Numbers</label>
              <div class="col-md-8">
                <input type="checkbox" class="custom-control-input" id="numbers" name="numbers" {{#if options.numbers}}checked{{/if}}>
                <label class="custom-control-label" for="numbers">(e.g. 123456)</label>
              </div>
            </div>

            <div class="form-group row mb-2 align-items-center">
              <label for="symbols" class="col-md-4 col-form-label">Include Symbols</label>
              <div class="col-md-8">
                <input type="checkbox" class="custom-control-input" id="symbols" name="symbols" {{#if options.symbols}}checked{{/if}}>
                <label class="custom-control-label" for="symbols">(e.g. @#$%)</label>
              </div>
            </div>
  
            <!-- Exclude Characters -->
            <div class="form-group row">
              <label for="excludeChars" class="col-md-4 col-form-label">Exclude Characters</label>
              <div class="col-md-8">
                <input type="text" class="form-control" id="excludeChars" name="excludeChars" value="{{options.excludeChars}}"> 
                <p class="hint" style="color: #888888; font-size:small">These characters will not appear in the password</p>
              </div>
            </div>

            {{!-- {{#if }} {和#之間不能空格!! --}}
            {{#if password }}
            <div class="alert alert-info mb-4" role="alert" style="background-color: #caecf2;">
              <span>Your password is: <code style="color: #f83598;"> {{password}} </code></span>
            </div>
            {{/if}}
            <button type="submit" class="btn mb-4" style="background-color: #00ab34; color:white">Generate Password</button>
          </form>

          <div class="card-footer text-end" style="background-color: #f6f6f6;">
            <p class="text-muted mb-0" style="font-size: 16px;">Idea is Inspired by <span style="color: #178aff;">Strong Random Password</span></p>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
  